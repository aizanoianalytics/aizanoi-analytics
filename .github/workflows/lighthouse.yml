name: Aizanoi Lighthouse

on:
  push:
    branches:
      - main
      - 'feat/**'
      - 'fix/**'
  pull_request:

permissions:
  contents: read

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Lighthouse
        run: npm install --no-save lighthouse@13.4.1

      - name: Start static frontend server
        run: |
          python3 -m http.server 4173 --directory frontend >/tmp/aizanoi-lighthouse-http.log 2>&1 &
          for i in {1..20}; do
            curl -fsS http://127.0.0.1:4173/ >/dev/null && exit 0
            sleep 0.5
          done
          cat /tmp/aizanoi-lighthouse-http.log
          exit 1

      - name: Enforce Lighthouse budgets
        run: |
          npx lighthouse http://127.0.0.1:4173/ \
            --quiet \
            --chrome-flags="--headless" \
            --output=json \
            --output-path=/tmp/aizanoi-lighthouse.json
          node tests/lighthouse-budget.mjs /tmp/aizanoi-lighthouse.json
