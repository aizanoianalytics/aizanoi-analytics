import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import { chromium } from 'playwright';
const base=process.env.ANCIENT_WORLD_BASE_URL||'http://127.0.0.1:4173';
const release=readFileSync('frontend/release.js','utf8');
const CURRENT_CACHE=release.match(/CACHE:\s*'([^']+)'/)?.[1];
assert.ok(CURRENT_CACHE,'release.js must declare CACHE');
const browser=await chromium.launch({headless:true}),context=await browser.newContext({serviceWorkers:'allow'}),page=await context.newPage();
try{await page.goto(`${base}/tv/`,{waitUntil:'networkidle'});await page.evaluate(async()=>{await caches.open('aizanoi-field-shell-v1');await caches.open('aizanoi-os-shell-v0.0.1');await navigator.serviceWorker.register('/service-worker.js',{scope:'/'});await navigator.serviceWorker.ready;});await page.reload({waitUntil:'networkidle'});await page.waitForFunction(()=>Boolean(navigator.serviceWorker.controller));const installed=await page.evaluate(async(cacheName)=>{const cache=await caches.open(cacheName);return{keys:await caches.keys(),current:(await cache.keys()).map((item)=>new URL(item.url).pathname)};},CURRENT_CACHE);assert.ok(installed.keys.includes(CURRENT_CACHE));assert.equal(installed.keys.includes('aizanoi-field-shell-v1'),false);assert.equal(installed.keys.includes('aizanoi-os-shell-v0.0.1'),false);for(const required of ['/','/release.js','/manifest.webmanifest','/js/v3/main.js','/styles/shell.css','/assets/branding/aizanoi-pwa-192.png'])assert.ok(installed.current.includes(required),`precache missing ${required}`);await page.goto(`${base}/journal/`,{waitUntil:'networkidle'});await context.setOffline(true);await page.reload({waitUntil:'domcontentloaded'});assert.equal((await page.locator('h1').textContent())?.trim(),'Aizanoi Journal');await context.setOffline(false);await page.evaluate(async()=>Promise.all(Array.from({length:40},(_,index)=>fetch(`/styles/landing.css?runtime=${index}`))));await page.waitForTimeout(250);const runtimeCount=await page.evaluate(async(cacheName)=>{const cache=await caches.open(cacheName),keys=await cache.keys();return keys.filter((request)=>new URL(request.url).search).length;},CURRENT_CACHE);assert.ok(runtimeCount<=24,`runtime cache was not pruned (${runtimeCount})`);console.log('service worker release metadata, install, cleanup, offline navigation and pruning passed');}finally{await context.close();await browser.close();}
