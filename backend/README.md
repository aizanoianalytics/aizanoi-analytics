# Aizanoi Analytics Backend

Node.js/Express API used by the Aizanoi Analytics frontend.

## Setup

```bash
cp .env.example .env
npm install
node server.js
```

The real `.env` is intentionally excluded from Git. Use environment variables for provider credentials and local configuration.
