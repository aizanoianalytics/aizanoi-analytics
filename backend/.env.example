# Copy to .env for local development. Never commit the real .env.
PORT=3001
NODE_ENV=development
SANDBOX_DIR=/var/lib/aizanoi/sandbox

# External AI integration was removed in the security build.
# Do not add provider API keys here unless AI is intentionally redesigned
# with a separate privacy, abuse-prevention and security review.
